# 安全说明

## 凭证

- 仅从环境变量读取 `FIRECRAWL_API_KEY` 等密钥。
- 不把 Cookie、API Key、页面会话或原始登录数据写入仓库。
- 测试使用本地模拟服务器，不调用真实付费接口。

## 文件安全

- 写入正文与状态时使用临时文件加原子替换。
- 默认不覆盖用户原稿；覆盖模式必须创建备份或显式输出到新目录。
- 项目名称和相对路径必须经过路径穿越校验。

## 报告问题

公开仓库发布前将补充安全联系渠道。不要在公开 Issue 中提交凭证或包含隐私的小说原稿。
